# PSEUDO CODE POUR LAB 02:
```
dx = saisie("la distance dx (km) ")
dy = saisie("la distance dy (km) ")
s1 = saisie("la vitesse sur la route s1 (km/h) : ")
s2 = saisie("la vitesse sur la route s2 (km/h) : ")
L1 = saisie("la longueur du premier segment (km) :")
L2 = racine(dx puissance 2 + dy puissance 2)
t1 = L1 / s1
t2 = L2 / s2
t_total = t1 + t2
ecrire(t_total)
```

